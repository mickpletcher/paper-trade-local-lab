services:
  tradeforge:
    build:
      context: .
    container_name: tradeforge
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
